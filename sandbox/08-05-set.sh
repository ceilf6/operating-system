# $1 如果没有 "" 会导致单词分割，原先一整个字符串被拆分为很多参数
# printf "%s\n" "$1" | tr -cs '[:alnum:]' '\n' | awk '!seen[$0]++'
# •	[:alnum:]：字母和数字
# •	-c：取反，也就是“不是字母数字的字符”
# •	-s：把连续的分隔符合并
# •	把这些非字母数字字符都替换成换行
# awk 对见过的筛选

printf "%s\n" "$1" | sed 's/[^A-Za-z][^A-Za-z]*/\n/g' | awk '!seen[$0]++'
# sed 将非字母换为换行后
# awk 去重