> 以 sandbox/13-...sh 为准

# tr

tr 命令用于字符处理

```bash
tr from to
```

按照 from 到 to 的规则，逐个字符处理

例如

```bash
tr ':' '|'
```

大小写转换

```bash
tr 'A-Z' 'a-z'
```

# useradd

```bash
useradd -u UID \
        -g GID \
        -d dir-home目录 \
        -c comment注释评论 \
        -s shell \
        -p passwd \
        用户名
```